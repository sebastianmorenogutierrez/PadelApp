## application.properties - CONFIGURACIÓN BASE

spring.application.name=HolaSpring
server.port = 8888
spring.main.banner-mode=off
spring.thymeleaf-cache=off
logging.level.org.springframework.web=DEBUG
logging.level.org.thymeleaf=TRACE

---

# ?? 1. CONFIGURACIÓN DE BASE DE DATOS (Fija en el archivo - USANDO EL URL PÚBLICO)
# Usamos el URL público (hopper.proxy.rlwy.net) porque el interno (mysql.railway.internal)
# a veces no está disponible en las primeras fases del despliegue.

spring.datasource.url=jdbc:mysql://hopper.proxy.rlwy.net:10394/railway
spring.datasource.username=root
spring.datasource.password=pCVdesdcsuAepNylLZjHxtYZlUlyvUiY
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? 2. CONFIGURACIÓN JPA / HIBERNATE
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging de SQL
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.security=DEBUG

---

# ? CONFIGURACIÓN DE CORREO (SMTP) - ACTIVADA SOLO EN PERFIL 'prod'
spring.config.activate.on-profile=prod

# Las variables de Correo (SPRING_MAIL_HOST, etc.) deben seguir en Railway.
spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE}